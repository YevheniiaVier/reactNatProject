import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 32,
    paddingLeft: 16,
    paddingRight: 16,
  },
  mapBox: {
    // flex: 1,
  },
  goBackBtn: {
    position: "absolute",
    // left: 16,
    // top: -34,
    // zIndex: 1,
  },
});
